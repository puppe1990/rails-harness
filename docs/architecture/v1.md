# Arquitetura v1

## Visão geral

A `v1` é a forma mais simples da aplicação. Tudo fica concentrado em `controllers` e `models`, com o Rails operando praticamente no padrão mais direto possível.

Essa versão funciona bem para uma base pequena, com poucas regras de negócio e baixo volume de endpoints. A vantagem é a previsibilidade: qualquer pessoa acostumada com Rails encontra rapidamente onde estão as ações HTTP e onde estão as entidades principais.

## Estrutura

```text
app
├── controllers
│   ├── application_controller.rb
│   ├── todos_controller.rb
│   └── users_controller.rb
└── models
    ├── application_record.rb
    ├── todo.rb
    └── user.rb
```

## Como pensar essa versão

- `controllers` concentram entrada HTTP e, na prática, tendem a acumular orquestração.
- `models` carregam dados e normalmente acabam absorvendo validações e regras.
- Não existe uma camada explícita para serialização, serviços, enums ou objetos auxiliares.

## Vantagens

- muito simples de entender
- pouco custo estrutural
- boa para prototipagem e CRUD básico
- segue a convenção Rails quase sem abstrações extras

## Limitações

- controllers tendem a crescer rápido
- regras de negócio ficam espalhadas entre controller e model
- difícil isolar casos de uso
- serialização e contratos de entrada ainda não estão bem definidos

## Quando usar

Use a `v1` quando o sistema ainda é pequeno, o domínio é simples e a prioridade é velocidade de entrega com o mínimo de estrutura possível.
