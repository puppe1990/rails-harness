# Arquitetura v6

## Visão geral

A `v6` é uma limpeza semântica da `v5`. Ela reduz sufixos excessivos e troca nomes longos por termos mais diretos. Por exemplo:

- `statuses_enum.rb` vira `statuses.rb`
- `password_value_object.rb` vira `password.rb`
- `create_service.rb` vira `create.rb`

O objetivo é assumir que o contexto da pasta já explica o papel do arquivo.

## Contexto acumulado até aqui

A base evolutiva até a `v5` já tinha resolvido vários problemas importantes:

- a `v1` era simples, mas concentrava demais as responsabilidades
- a `v2` separou saída e casos de uso
- a `v3` organizou casos de uso por domínio
- a `v4` deu nomes e lugares para enums, params e value objects
- a `v5` reorganizou tudo isso em subpastas por domínio dentro de cada categoria técnica

Depois desse ponto, o problema principal deixa de ser estrutural e passa a ser semântico. A `v5` já é mais organizada, mas ainda carrega muita redundância nos nomes. Como o contexto já está nas pastas, manter nomes como `create_service.rb` e `password_value_object.rb` repete informação desnecessária.

A `v6` é, portanto, uma refatoração de linguagem e convenção. Ela simplifica o vocabulário sem alterar o modelo arquitetural de fundo.

## Estrutura

```text
app
├── controllers
│   ├── application_controller.rb
│   ├── todos_controller.rb
│   └── users_controller.rb
├── enums
│   ├── todo
│   │   └── statuses.rb
│   └── user
│       └── roles.rb
├── models
│   ├── application_record.rb
│   ├── todo.rb
│   └── user.rb
├── permitted_params
│   ├── todo
│   │   └── permitted_params.rb
│   └── user
│       └── permitted_params.rb
├── serializers
│   ├── todo
│   │   └── serializer.rb
│   └── user
│       └── serializer.rb
├── services
│   ├── todo
│   │   ├── complete.rb
│   │   ├── create.rb
│   │   ├── destroy.rb
│   │   ├── find.rb
│   │   ├── find_all.rb
│   │   ├── uncomplete.rb
│   │   └── update.rb
│   └── user
│       └── create.rb
└── value_objects
    └── user
        └── password.rb
```

## O que melhora em relação à v5

- nomes ficam mais curtos
- a leitura melhora para quem já entende a convenção da pasta
- reduz ruído sem perder organização

## Leitura arquitetural

A `v6` mostra uma mudança importante de maturidade do projeto: a arquitetura já confia na sua própria convenção. Em vez de depender de nomes longos para explicar tudo, ela assume que:

- a pasta já comunica o domínio
- a posição do arquivo já comunica sua função
- a legibilidade vem do conjunto, não do nome isolado

## Vantagens

- convenções mais elegantes
- menos repetição nos arquivos
- estrutura mais agradável para manutenção diária

## Limitações

- exige disciplina de naming e namespacing
- para quem chega novo, `create.rb` é menos autoexplicativo fora do contexto
- a estrutura ainda está espalhada por tipo técnico

## Quando usar

Use a `v6` quando o time já confia na convenção baseada em pastas e quer reduzir verbosidade sem abrir mão da organização por domínio.
